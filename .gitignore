# Build output - regenerated by src/normalize.mjs, published as a
# versioned artifact by the scheduled workflow, never committed here.
dist/

# The real Kaikki extract - fetched by the scheduled workflow, never
# committed (only data/sample.jsonl, a small smoke-test fixture, is
# tracked).
data/dictionary-Yoruba.jsonl
data/*.jsonl.gz

node_modules/
*.log
.DS_Store
